# requirements.txt

# For the web server (MCP)
fastapi
uvicorn[standard]

# For the filesystem watcher
watchdog

# For scheduled tasks (ZFS snapshots)
apscheduler

# For PostgreSQL async database connectivity
asyncpg

# Note: joshua-libs (joshua_network + joshua_logger) installed via Dockerfile
